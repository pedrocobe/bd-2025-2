# Database Configuration
DB_HOST=localhost
DB_PORT=5435
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=ecommerce_exam

# JWT Configuration
JWT_SECRET=your-secret-key-change-this-in-production
JWT_EXPIRES_IN=24h

# Application
PORT=3000
NODE_ENV=development

# Mock Mode - Set to 'true' to use mock data instead of real database
# IMPORTANT: Set to 'false' when you're ready to test your SQL queries!
USE_MOCKS=true
