# Database Configuration
DB_HOST=localhost
DB_PORT=5435
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=ecommerce_exam

# JWT Configuration
JWT_SECRET=exam-secret-key-2025
JWT_EXPIRES_IN=24h

# Application
PORT=3000
NODE_ENV=development

# Mock Mode - Set to 'true' to use mock data instead of real database
# This allows students to see how the API should work before writing SQL
USE_MOCKS=false
